// Barrel — consumers (the scan uploader today; the app router/nav in T-057)
// import from here only, never individual auth feature files directly.
export { RegisterForm } from './components/register-form';
export { LoginForm } from './components/login-form';
export { RegistrationWall } from './components/registration-wall';
export { GuestScansRestoredBanner } from './components/guest-scans-restored-banner';

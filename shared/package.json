{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod contract schemas + inferred TypeScript types (single source of truth for API DTOs).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}

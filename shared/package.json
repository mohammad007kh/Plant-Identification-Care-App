{
  "name": "shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod contract schemas + inferred TypeScript types. Populated by T-013.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "echo \"shared build not configured yet (see T-013)\" && exit 0"
  }
}
